{% extends "base.html" %}
{% block content %}
	<form action="" method="post" name="login">
		{{form.hidden_tag()}}
	
		<h2>Agregar grupos de paquetes extras</h2> <br /><br />
		<div class="container well">
			<div class="row">
			{% for value_form in form.check%}
				<div class="col-xs-12 col-sm-4">
					<input type="checkbox" 
					name={{value_form[0]}} value="{{value_form[0]}}" 
					rel="tooltip" title="{{value_form[1]}}"
					/> {{value_form[0]}}
				</div>
			{%endfor%}
			</div>
		</div>
		<button  name ="atr2" value="True"  type="submit"  class="btn btn-primary btn-lg">atras</button>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
		<button name ="gen" value="True" type="submit"  class="btn btn-primary btn-lg">Generar</button><br />
	</form>
{% endblock %}